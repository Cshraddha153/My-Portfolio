# Base URL for the API. Leave empty in development — the Vite dev server
# proxies /api requests to the Express server automatically.
# In production, set this to the deployed API origin, e.g. https://api.example.com
VITE_API_BASE=
